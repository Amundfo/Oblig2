name: github-actions
on: push

jobs:
    build:

     runs-on: ubuntu-latest
     steps: 
     - uses: actions/checkout@v2
     - name: My first step
     - uses: actions/Test@latest
     - name: Test
